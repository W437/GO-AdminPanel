APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:/NC6CNBiDJb2vV4fRviEsMqy5gKbePRgk44JGkZFAYY=
APP_DEBUG=true
APP_URL=http://localhost
APP_MODE=dev

# API Key for mobile app authentication (generate with: openssl rand -base64 32)
API_KEY=

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=go
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=null
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=

# OpenAI API for AI-powered translation
# Get your API key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_TRANSLATION_MODEL=gpt-4o-mini

# Parallel Translation Settings
# Leave OPENAI_ITEMS_PER_ITERATION empty to auto-calc workers × batch size
OPENAI_PARALLEL_WORKERS=20
OPENAI_BATCH_SIZE=200
OPENAI_ITEMS_PER_ITERATION=
OPENAI_TIMEOUT=60
OPENAI_MAX_RETRIES=2
OPENAI_RETRY_DELAY_MS=750

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Story feature configuration
STORY_ENABLED=true
STORY_MAX_MEDIA=10
STORY_DEFAULT_DURATION=5
STORY_RETENTION_DAYS=7
STORY_MEDIA_DISK=public
STORY_ENABLE_VIDEO_PROCESSING=true

# ============================================================================
# PHP FILE UPLOAD CONFIGURATION
# ============================================================================
# Configure PHP upload limits for file and video uploads
# These values are applied at runtime via AppServiceProvider

# Maximum File Upload Size (in megabytes or with suffix like 20M)
# Must be less than or equal to POST_MAX_SIZE
# For banner videos, this should be at least 20M
PHP_UPLOAD_MAX_FILESIZE=20M

# Maximum POST Data Size (in megabytes or with suffix like 25M)
# Should be slightly larger than UPLOAD_MAX_FILESIZE
PHP_POST_MAX_SIZE=25M

# Maximum Script Execution Time (in seconds, 0 = unlimited)
# Increase for large file uploads or long-running operations
PHP_MAX_EXECUTION_TIME=300

# Maximum Input Time (in seconds, -1 = unlimited)
# Maximum time to parse input data
PHP_MAX_INPUT_TIME=300

# Memory Limit (in megabytes or with suffix like 256M, -1 = unlimited)
# Increase for image/video processing operations
PHP_MEMORY_LIMIT=256M
